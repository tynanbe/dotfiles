default_host_triple = "x86_64-unknown-linux-

{{- if (eq "oak" .chezmoi.hostname) -}}
  {{- "gnu" -}}
{{- else -}}
  {{- "musl" -}}
{{- end -}}

{{""}}"
default_toolchain = "nightly"
profile = "default"
version = "12"

[overrides]
